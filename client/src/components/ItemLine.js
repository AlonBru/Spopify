import React,{useState} from 'react';

function ItemLine({target}) {
    const [list,setList] = useState([])

  return (
      <div>
        {list.map(ele =>{

        })}
      </div>
  )
}

export default ItemLine;
